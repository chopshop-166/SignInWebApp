{% extends "base.html.jinja2" %}
{% from 'bootstrap5/utils.html' import render_static %}
{% block title %}Event {{event.name}} - Chop Shop Sign In{% endblock %}
{% block content %}
<div class="container table-responsive">
  <div class="row">
    <div class="col">
      <h1>{{event.name}}</h1>
      <h2>{{event.type_.name}}</h2>
      {%- if event.descripion -%}
      <p>Description: {{event.description}}</p>
      {%- endif -%}
      <p>Location: {{event.location}}</p>
      <p>{{event.start}} - {{event.end}}</p>
    </div>
    <div class="col">
      <table id="users" class="table table-striped table-hover table-dark">
        <thead>
          <tr>
            <th scope="col">User</th>
          </tr>
        </thead>
        <tbody id="userbody">
        {%- for username, usertime in users -%}
          <tr>
            <td>{{username}}</td>
            <td>{{usertime}}</td>
          </tr>
        {%- endfor -%}
        </tbody>
      </table>
    </div>
  </div>
</div>
{% endblock %}